# Security Policy

## 项目状态

CSGDB 尚未发布稳定版本。设计文档中的加密、密钥轮换、权限隔离和恢复能力是实现目标，不代表当前仓库已经提供可用于生产环境的安全保证。

## 报告漏洞

请优先使用 GitHub Security Advisories 的私密漏洞报告功能，不要在公开 Issue 中披露以下内容：

- 密钥泄露或密钥生命周期缺陷；
- 数据库、WAL、临时文件、索引或备份中的明文泄露；
- 加密 Nonce 重用、认证绕过或降级；
- 越权读取其他 Agent 命名空间；
- 查询预算绕过导致的拒绝服务；
- 恶意数据库文件导致的内存安全问题；
- 恢复、迁移或压缩过程中的数据破坏。

报告中请包含受影响版本或提交、复现步骤、影响范围和建议修复方向。维护者确认问题后再协调披露时间。

## 威胁模型

首期安全模型主要防护：

- 设备丢失后数据库文件被离线复制；
- 备份文件泄露；
- 不可信 Agent 生成恶意动态查询；
- 数据库文件损坏或被恶意修改；
- 多 Agent 共用数据库时的越权访问。

首期不承诺抵抗已经取得进程内任意代码执行、内核控制权或能够读取解锁后进程内存的攻击者。

## 加密实现要求

- 不自行设计密码学原语；
- 只采用公开审查、维护活跃的实现；
- 密钥与普通数据使用不同类型和内存路径；
- 主库、WAL、临时文件、索引、Blob 与备份不得出现覆盖缺口；
- 密钥轮换、崩溃恢复和错误密钥必须具有测试用例；
- 自定义加密 VFS 在独立审计前不得标记为生产可用。
